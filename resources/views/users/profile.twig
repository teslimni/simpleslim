{% extends 'layout/app.twig' %}
{% block content %}
	{% if not user %}
		<p>No user found</p>
		{% else %}
			<h1> {{user.username}} </h1>
			<ul>
				<li>{{ user.name }}</li>
				<li>{{ user.email }}</li>
			</ul>
	{% endif %}
{% endblock %}